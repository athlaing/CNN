#!/usr/bin/env python
# -*- coding: utf-8 -*-
from setuptools import setup, find_packages


setup(name='pytorch',
      version="1.0",
      description='Training to extract parameters',
      packages=find_packages(exclude=[]),
      entry_points={
      },
      include_package_data=True,
      )
